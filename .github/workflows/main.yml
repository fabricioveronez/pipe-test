name: Exemplo Output
run-name: Executando workflow para output por ${{ github.actor }}
on:
  repository_dispatch: 
    types:
      - dependency_updated
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Listar os arquivos nesse momento
        run: echo "Disparou o evento"
