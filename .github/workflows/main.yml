name: Workflow Evento
on:
  push: 
    branches: ["main", "feature/*"]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Action de teste
        run: echo "Actions executada hotfix"
