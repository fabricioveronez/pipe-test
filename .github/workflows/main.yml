name: Workflow Evento
on:
  pull_request:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions: 
      issues: write
    steps:
      - name: Adicionando funcionalidade
        run: echo "Teste de action"
      - name: Action de teste
        uses: mshick/add-pr-comment@v2
        with:
          message: |
            Adicionando uma mensagem no PR
