name: Workflow disparado 
run-name: "Workflow disparado por ${{ github.run_number }}"
on:
  workflow_dispatch: 
  push: 
concurrency: 
  group: ${{ github.ref }}-${{ github.workflow }}
  cancel-in-progress: true
jobs:
  exemplo-fluxo-execucao-01:
    runs-on: ubuntu-latest
    steps:
      - name: Execução do job de teste
        run: |
          sleep 120;
          echo "Action executada job 01"
# tesste