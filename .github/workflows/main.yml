name: Workflow disparado 
run-name: "Workflow disparado por ${{ github.actor }}"
on:
  workflow_dispatch: 
jobs:
  exemplo-fluxo-execucao-01:
    concurrency: pipeline-aula
    runs-on: ubuntu-latest
    steps:
      - name: Execução do job de teste
        run: |
          sleep 120;
          echo "Action executada job 01"
  exemplo-fluxo-execucao-02:
    runs-on: ubuntu-latest
    needs: [exemplo-fluxo-execucao-01]
    steps:
      - name: Execução do job de teste
        run: |
          sleep 120;
          echo "Action executada job 02"
        
